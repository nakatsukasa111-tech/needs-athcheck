# NeeDS フィジカルチェック — 作業メモ

このファイルは、このリポジトリで作業するときに最初に読む「これまでの決定事項」です。
仕様の詳細は `docs/CLAUDE_CODE_指示書.md`、採点とチャートの考え方は
`docs/採点とチャートの作り方.md` にあります。

---

## このアプリは何か

神戸のパーソナルトレーニングジム NeeDS の、アスリート向けフィジカルチェック・アプリ。
紙の「NeeDSスコア記入用紙」40項目をタブレットから入力し、自動採点して見せる。

見せる相手は2種類あり、目的が違う。

- **体験者（初回）**：レーダーチャートで身体の「形」と伸びしろを見せる。営業ツールの側面を持つ
- **既存会員（2回目以降）**：推移グラフで変化を見せる。管理ツール

入力するのはトレーナー（タブレット想定）、見せる相手はアスリート本人。

## 構成

```
index.html（GitHub Pages・単一ファイル）
  ↓ GET  doGet  … 閾値の取得 / 会員検索 / 履歴取得
  ↓ POST doPost … 会員登録 / 測定の保存
apps-script/コード.gs（ウェブアプリとして公開）
  ↓
Google スプレッドシート「NeeDS フィジカルチェック データ」
  members / measurements / thresholds / rules
```

- 公開URL： https://nakatsukasa111-tech.github.io/needs-athcheck/
- `main` に push すると GitHub Pages が自動で再ビルドされる（40秒ほど）
- 接続先URLと合言葉（`NeeDS2026`）は `index.html` の `DEFAULT_API_URL` / `DEFAULT_TOKEN` に
  初期値として埋め込み済み。画面の［接続設定］で上書きでき、localStorage が優先される

## 崩してはいけない設計原則

1. **閾値をコードに埋め込まない。** `thresholds` / `rules` シートから起動時に読む。
   中務さんがスプレッドシートの数値を書き換えるだけで判定基準が変わる状態を保つ
2. **未入力の項目は分母から除外する。** ゼロ点として平均に入れない。
   体験時は一部の項目しか測らないため、これは必須
3. **実測値とスコアの両方を保存する。** 表示は保存済みスコアではなく、
   保存された実測値を現行の基準で計算し直して描く。閾値を調整しても過去の推移が壊れない。
   保存済みスコアは「当時どう見せたか」の記録として残す
4. **年齢・年齢区分・除脂肪体重・回数はサーバー（Apps Script）側で計算する。**
   アプリ側の計算違いが混入するのを防ぐため
5. **履歴は名前でなく `member_id` で紐づける。** 表記ゆれ・同姓同名・改姓で履歴が分断されるため。
   UIだけ「名前で呼び出す」操作感にする
6. **デモ用の数値はスプレッドシートに書き込まない**

## 中務さんが決めたこと

| いつ | 何を | 決めたこと |
|---|---|---|
| 初回 | 接続設定 | 接続先は確定なのでアプリに初期値として埋め込む。設定画面は残して後から変更できるようにする |
| 初回 | 閾値データ | thresholds / rules はスプレッドシートに取り込み済み。xlsx は列構造の参考資料であり、アプリはシートから直接読む |
| 結果画面 | 項目一覧 | 「項目ごとの結果」の表は残す（数値を一覧で確認したいため） |
| 結果画面 | 部門カード | 部門は縦に並べる（タブで切り替えず、スクロールで全部たどれるように） |
| 部門レーダー | つながりの式 | 「100 − 部位間の差」と「2部位の平均」の低いほう。差が大きいと下がり、両方低ければそれ以上には上がらない |
| 部門レーダー | バランスの差 | 0点差=100（緑）／1点差=66.7（黄）／2点以上=33.3（赤） |
| 部門レーダー | ジャンプ | 跳躍高／反動の使い方／腕振りの連動／片脚／RSI の5軸 |
| デモ | 保存の扱い | デモの数値は保存しない。ボタンは常に表示し、入力済みなら確認ダイアログを出す |
| デモ | 架空の履歴 | 推移グラフも動いて見えるよう、架空の過去3〜5回分も作る（画面上だけ） |

### まだ決まっていないこと

- **レーダー主役／総合点主役**：切り替えできる状態で保留（`DISPLAY.hero`）
- **解説文の温度感**：事実ベース／伸びしろ重視の2つを用意して切り替え可能（`TEXTS`）
- **「動きの質」のレーダー**：軸が未定のため、棒グラフのみ
- **軸の並び順**：柔軟性・筋力は部位とつながりを交互に並べている（(a)〜(e)順に戻す選択肢あり）
- **片脚立位・地面タッチの閾値**：NeeDS独自プロトコルで参照文献がなく仮置き。
  実測データが20〜30件たまった時点で分布から引き直す前提

## 実装上の落とし穴（実際に踏んだもの）

- **グローバル名の衝突**。`var history = []` は `window.history` と衝突して strict モードで即死する
  （`sessions` に改名済み）。`function pick()` を二重に定義して、選手を読み込む関数が
  上書きされたこともある。**関数名・変数名を足したら重複を確認すること**
- **Chart.js のキャンバスは必ず高さを固定した箱（`.chartBox`）の中に置く。**
  `maintainAspectRatio:false` で高さの決まっていない親に置くと、
  キャンバスが親を広げ→親に合わせて広がる、の循環で画面が下に伸び続ける
- **要素単位のスクリーンショットは再描画を誘発する**ので、グラフが途中の絵で写る。
  検証で撮るときは `Chart.defaults.animation=false` にしてから撮る
- Apps Script は POST の `Content-Type` を `text/plain` にする（preflight を飛ばさないため）
- 「コードを直したのにデプロイし直していない」が接続トラブルの最頻出原因。
  `API_VERSION` を返す `ping` を用意してあるので、接続テストで版を確認できる

## 変更したときの確認

外部サービスに繋がずに確認できるよう、`fetch` を差し替えてブラウザ（Chromium + Playwright）で
動かす方法をとってきた。最低限、次を見ること。

- 選手を選ぶ → 40項目を入力 → 採点 → 保存 まで通るか
- 保存が二重に飛ばないか
- iPad幅（820px）・スマホ幅（390px）で横スクロールが出ないか、ページ高が伸び続けないか
- デモを何回か押して、点数が現実的にばらけるか

このサンドボックスからは `script.google.com` と `github.io` に接続できない
（ネットワークポリシーで遮断）。実機での最終確認は中務さんにお願いすること。
